import { CreateAccount } from "@components/auth";
import { Layout } from "@components/common";

const CreateAccountPage = () => {
  return <CreateAccount />;
};

CreateAccountPage.Layout = Layout;
export default CreateAccountPage;
