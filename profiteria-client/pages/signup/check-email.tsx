import { CheckEmail } from "@components/auth";
import { Layout } from "@components/common";

const CheckEmailPage = () => {
  return <CheckEmail />;
};

CheckEmailPage.Layout = Layout;
export default CheckEmailPage;
