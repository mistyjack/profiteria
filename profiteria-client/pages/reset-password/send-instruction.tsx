import { SendInstruction } from "@components/auth";
import { Layout } from "@components/common";

const SendInstructionPage = () => {
  return <SendInstruction />;
};
SendInstructionPage.Layout = Layout;
export default SendInstructionPage;
